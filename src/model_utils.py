"""Model loading helpers for Whisper-based ASR."""

from __future__ import annotations

from typing import Any


def load_model(model_name: str) -> Any:
    """Placeholder model loader.

    Replace this function with the actual Hugging Face / Transformers
    loading logic used in the study.
    """
    raise NotImplementedError(
        "Replace `load_model` with the actual Whisper model loading implementation."
    )
